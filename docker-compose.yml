version: '3.8'

services:
  solver:
    build: .
    container_name: lcomp-solver
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    command: python main.py
