version: '3.8'

services:
  solver:
    build: .
    container_name: lcomp-solver
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    command: python solver.py
